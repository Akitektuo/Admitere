#include <iostream>
using namespace std;

const int MIN = -100000;
const int MAX = 100000;

int abp(int nodeIndex, int depth, int values[], int alpha, int beta, bool maximazingPlayer) {
    if (depth == 0) {
        return values[i];
    }
    if (maximazingPlayer) {
        int maxVal = MIN;
        for (int i = 0; i < position; i++) {

        }
    }
}

int main() {
    int n = 0, vals[10000] = {0};
    cout << abp(0, 0, true, vals, MIN, MAX);
    return 0;
}
